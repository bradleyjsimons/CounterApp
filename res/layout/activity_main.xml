

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".CounterActivity" >
    
    <TextView
        android:id="@+id/welcomeLabel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/welcome_text"
        android:textSize="35sp" />

    <Button
        android:id="@+id/loadCounter"
        android:layout_width="wrap_content"
        android:layout_height="75sp"
        android:layout_alignLeft="@+id/addNewCounter"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/addNewCounter"
        android:layout_marginBottom="30dp"
        android:onClick="loadCounter"
        android:text="@string/load_counter"
        android:textSize="25sp" />

    <Button
        android:id="@+id/addNewCounter"
        android:layout_width="wrap_content"
        android:layout_height="75sp"
        android:layout_above="@+id/loadCounter"
        android:layout_alignLeft="@+id/welcomeLabel"
        android:layout_alignRight="@+id/welcomeLabel"
        android:onClick="addNewCounter"
        android:text="@string/add_counter"
        android:textSize="25sp" />
        
</RelativeLayout>
